/**
 * PostgreSQL schema, migrations, and repositories.
 * A concrete client (e.g. Prisma, Drizzle, or pg) gets wired up here
 * once the persistence approach is decided in a follow-up issue.
 */
export {};
